import * as React from 'react';
import Button from '@mui/material/Button';

export default function AddAssignmentButton() {
  return (
      <Button variant="outlined" style={{ float: 'right' }}>Add Assignment</Button>
  );
}